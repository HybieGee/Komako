---
import BaseLayout from '@layouts/BaseLayout.astro';
import { useTranslations } from '@lib/i18n';

const lang = 'ja';
const t = useTranslations(lang);
---

<BaseLayout
  title={`${t('journal.title')} - コマコギャラリー`}
  description={t('journal.subtitle')}
  lang={lang}
>
  <div class="container py-16">
    <div class="text-center mb-16">
      <h1 class="text-4xl md:text-5xl font-serif font-bold text-komako-eyes mb-4">
        {t('journal.title')}
      </h1>
      <p class="text-xl text-komako-soft max-w-2xl mx-auto">
        {t('journal.subtitle')}
      </p>
      <div class="w-16 h-0.5 bg-gradient-to-r from-accent-rose to-accent-pink mx-auto mt-6"></div>
    </div>

    <!-- Journal posts would be displayed here -->
    <div class="max-w-4xl mx-auto space-y-8">
      <!-- Journal post cards would go here -->
    </div>
  </div>
</BaseLayout>